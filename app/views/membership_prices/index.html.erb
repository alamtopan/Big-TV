<div class="row">
  <div class="col-sm-12">
    <ol class="breadcrumb">
      <li>
        <i class="clip-home-3"></i>
        <%= link_to "Dashboard", root_path %>
      </li>
      <li class="active">
        Membership Price
      </li>
      <li class="search-box">
        <form class="sidebar-search">
          <div class="form-group">
            <input type="text" placeholder="Start Searching..." data-default="130">
            <button class="submit">
              <i class="clip-search-3"></i>
            </button>
          </div>
        </form>
      </li>
    </ol>
    <div class="page-header">
      <h1>
        List Membership Price 
        <div class="pull-right">
          <%= link_to '<i class="icon-plus"></i>Add Item'.html_safe, new_membership_price_path, class: "btn btn-primary" %>
        </div>
      </h1>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-md-12">
    <div class="panel panel-default">
      <div class="panel-heading">
        <i class="icon-external-link-sign"></i>
        Membership Price
        <div class="panel-tools">
          <a class="btn btn-xs btn-link panel-expand" href="#">
            <i class="icon-resize-full"></i>
          </a>
          <a class="btn btn-xs btn-link panel-collapse collapses" href="#">
          </a>
        </div>
      </div>
      <div class="panel-body">
        <table class="table table-hover" id="sample-table-1">
          <thead>
            <tr>
              <th>No</th>
              <th>Name Membership</th>
              <th>Price</th>
              <th>Periode</th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% @membership_prices.each_with_index do |m_price,index| %>
              <tr>
                <td><%= index+1 %></td>
                <td><%= m_price.membership.name %></td>
                <td><%= m_price.price %></td>
                <td><%= m_price.periode_time %></td>
                <td>
                  <div class="visible-md visible-lg hidden-sm hidden-xs pull-right">
                    <%= link_to '<i class="icon-edit"></i> Edit'.html_safe, edit_membership_price_path(m_price), class: "btn btn-teal" %>
                    <%= link_to '<i class="icon-remove"></i> Delete'.html_safe, membership_price_path(m_price), method: 'delete',class: "btn btn-bricky" ,data: { confirm: "Are you sure?" } %>
                  </div>
                </td>
              </tr>  
            <% end %>
          </tbody>
        </table>
        <div>
          <%= paginate @membership_prices %>
        </div>
      </div>
    </div>
  </div>
</div>