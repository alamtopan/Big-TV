<!DOCTYPE html>
<html>
  <head>
    <title>BigTv : <%= @title_page || "Big Experience"%></title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, user-scalable=0">
    <script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=false"></script>
    <%= favicon_link_tag 'icon.jpg' %>
    <% if current_user %>
      <%= stylesheet_link_tag    "application", :media => "all" %>
    <% else %>
      <%= stylesheet_link_tag    "application_front", :media => "all" %>
    <% end %>
  </head>
  <body>
    <% if current_user %>
      <%= render partial: "shareds/back_office/header_bar" %>
      <div class="main-container">
        <%= render partial: "shareds/back_office/side_bar" %>
        <div class="main-content">
          <div class="container" style="min-height: 760px;">
            <%= yield %>
          </div>
        </div>
      </div>
      <%= render partial: "shareds/back_office/footer" %>
      <%= javascript_include_tag "application" %>
    <% else %>
      <%= render partial: "shareds/front/header_detail_lanjut" %>
      <div class="top_banner">
        <div>
          <%= @top_content.description.html_safe %>
        </div>
      </div>
      <div class="cbp-fbscroller">
        <%= yield %>
        <%= render partial: "shareds/front/footer" %>
      </div>
      <%= javascript_include_tag "application_front" %>
      <%= render 'shareds/front/remarketing_script' %>
    <% end %>
  </body>
</html>